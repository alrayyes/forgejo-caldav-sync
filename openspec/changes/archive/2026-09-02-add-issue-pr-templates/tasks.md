Retroactive record of already-completed work (issue #9). Shipped on
`main` before this change was documented in OpenSpec.

## 1. Templates

- [x] 1.1 Add `bug_report.yml` with required `description`,
      `reproduction`, `expected` fields — verified by opening the
      repo's "New issue" page and confirming the form renders with
      those fields required
- [x] 1.2 Add `feature_request.yml` with required `description`,
      `acceptance_criteria`, `definition_of_done` fields — verified
      the same way
- [x] 1.3 Add `PULL_REQUEST_TEMPLATE.md` with a summary section and a
      test-plan checklist — verified by opening a new PR against this
      repo and confirming the description box is pre-filled
