version: "2"
linters:
  default: standard
  enable:
    - bodyclose
    - errorlint
    - gocritic
    - gocyclo
    - gosec
    - misspell
    - revive
    - unconvert
    - whitespace
  settings:
    gocyclo:
      min-complexity: 15
formatters:
  enable:
    - gofumpt
    - goimports
